{
    indir: ..
    prep: "
        echo Should be in top level
        ls
    "
}

{
    indir: "./test"
    prep: "
        echo Should be in test
        ls
    "
}

{
    indir: ..
    prep: "
        echo Should be in top level
        ls
    "
}
